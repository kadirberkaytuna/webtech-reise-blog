name: CI

on:  [ push ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        name: Set up JDK 17
        with:
          distibution: "adopt"
          java-version: 17
        name: clean build
        run: ./gradlew clean build --info --stacktrace